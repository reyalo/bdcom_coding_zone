#include <stdio.h>

enum Days {
    SUNDAY = 1,
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY, 
    FRIDAY,    
    SATURDAY 
};

int main() {
    // int response = ERROR;

    // if (response == ERROR) {
    //     printf("Operation not failed!\n");  // Output: Operation failed!
    // }

    printf("%d\n",MONDAY);

    return 0;
}
